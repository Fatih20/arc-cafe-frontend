import styled from "styled-components";

const Main = styled.div`
    background-color: red;
    height: 200px;
`;

function Store() {
    return (
        <Main id="Store" />
    );
}

export default Store;